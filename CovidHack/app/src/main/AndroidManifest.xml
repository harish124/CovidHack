<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bourbon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.harish_activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.clement_activities.Main2Activity"></activity>
        <activity android:name=".activities.clement_activities.startact"></activity>
        <activity android:name=".activities.clement_activities.verifi"></activity>
        <activity android:name=".activities.clement_activities.YoutubeList"></activity>
        <activity android:name=".activities.arumugam_activities.MainActivity"></activity>

        <receiver android:name=".activities.arumugam_activities.GeofenceBroadcastReceiver"/>
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

</manifest>